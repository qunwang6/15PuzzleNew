/// <reference types="node" />
import { PostHTML } from "posthtml";
export declare function replaceHash(str: string, buffer: Buffer, exp: RegExp, hashLength: number): string;
declare function plugin(options?: {
    path?: string;
    hashLength?: number;
    pattern?: RegExp;
}): (tree: PostHTML.Node) => void;
export default plugin;
export { plugin as hash, plugin as posthtmlHash };
